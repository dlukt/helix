// Package oauth provides Twitch OAuth helpers, clients, and token sources.
//
// It supports authorization-code, client-credentials, implicit-flow URL
// generation, device-code polling, token validation, token revocation, and
// refresh-aware token sources for use with the helix package.
package oauth
